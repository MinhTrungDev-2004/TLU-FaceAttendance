import 'package:flutter/material.dart';

class HeaderLecturer extends StatelessWidget {
  const HeaderLecturer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: double.infinity,
      color: const Color(0xFF1675FF),
    );
  }
}
